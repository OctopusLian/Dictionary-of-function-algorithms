#include <stdio.h>
#include <math.h>
void main ()
{
	printf ("不大于8.2的最大整数是: %.1lf\n", floor (8.2) );
	printf ("不大于2.8的最大整数是: %.1lf\n", floor (2.8) );
	printf ("不大于-5.3的最大整数是: %.1lf\n", floor (-5.3) );
	printf ("不大于-5.7的最大整数是: %.1lf\n", floor (-5.7) );
}
